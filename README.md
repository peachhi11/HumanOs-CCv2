# HumanOS Reviewer Modes

This branch holds the canonical HumanOS reviewer-mode guidance.

Primary doc:

- `docs/evaluation/humanos-reviewer-modes.md`

Related framework surfaces:

- `evaluation/scorecards` for score categories and verdict contract
- `docs/architecture` for the full layer model
- `docs/authoring` for authoring-time placement and separation rules
