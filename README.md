# HumanOS World Boundaries

This branch holds the canonical HumanOS world-boundary guidance.

Primary doc:

- `docs/world/humanos-world-boundaries.md`

Related framework surfaces:

- `docs/architecture` for the full layer model
- `docs/memory-retrieval` for retrieval and lorebook placement
- `docs/authoring` for contributor workflow and placement guidance
